spring:
  logging:
    level:
      io:
        pivotal: DEBUG
  datasource:
    url: jdbc:postgresql://localhost:5432/commands
    username: postgres
    password: 39409832
  jpa:
    hibernate:
      ddl-auto: create-drop
#  sql:
#    init:
#      mode: always